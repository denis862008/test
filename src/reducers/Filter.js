const initialState = {
    filter: 'SHOW_ALL'
};

function filter (state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}

export default filter;
